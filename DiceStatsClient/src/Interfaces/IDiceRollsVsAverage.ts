export default interface IDiceRollsVsAverage {
  20: { above: number; below: number };
  12: { above: number; below: number };
  10: { above: number; below: number };
  8: { above: number; below: number };
  6: { above: number; below: number };
  4: { above: number; below: number };
}
